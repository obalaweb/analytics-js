import React from 'react';
import type { RealtimeData } from '../types';
interface RealtimeCardsProps {
    realtime: RealtimeData | null;
}
export declare function RealtimeCards({ realtime }: RealtimeCardsProps): React.JSX.Element;
export {};
